# Secrets. .env.example is the committed template and holds no real key.
.env
.env.*
!.env.example

# Vercel CLI build cache — stale output here silently overrides config edits
.vercel

node_modules/
*.log

.DS_Store
Thumbs.db
